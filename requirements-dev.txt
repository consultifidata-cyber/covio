# requirements-dev.txt -- RE-3 (Release Engineering, Code Quality Gates).
# Development-only tooling for the backend (server/, test/native/) -- never
# needed to actually RUN server.py (see README.md's own `pip install flask`
# instruction for that). Install with: pip install -r requirements-dev.txt
#
# EXACT PIN, and it has to stay exact. ruff is a BLOCKING gate, and every
# ruff release can add or tighten lint rules -- with a `>=` bound, a routine
# upstream release turns CI red across the whole repo without a single line
# of this project's code changing. That is precisely what happened before:
# the gate was configured but never executed, and by the time it first ran
# it reported 70 violations and skipped the Publish Release job, so no
# release was ever published by CI.
#
# Verified: the tree is clean under `ruff check server/ test/native/` AND
# `ruff format --check` at this exact version. Bump deliberately, re-run
# both, and fix whatever the new version finds -- never widen the bound to
# make a failure go away.
ruff==0.16.2

# RISK-16 remediation (OTA manifest authenticity): server/tools/sign_manifest.py
# (release-signing tool, never shipped in firmware) and its test,
# test/native/test_sign_manifest_tool.py, need real ECDSA/PEM support.
# Confirmed working this session at v49.0.0; loose lower bound for the same
# reason as ruff above (no CI-verified exact pin has ever existed for this
# project's Python dependencies).
cryptography>=42.0.0
