# 13 — End-to-End Reconciliation

## Scope of this phase's reconciliation

This phase's two workstreams (RISK-04 fault-injection harness, RISK-15
anti-downgrade) are both firmware-side logic with no new server/database
interaction — neither introduces new `records`/`quarantined_records` rows.
The reconciliation identity from phase 1 (`Docs/audit/coviu_oil_meter_p0_remediation/08_DATABASE_IDEMPOTENCY_AND_RECONCILIATION.md`)
is **unchanged and re-confirmed**: RISK-01's fix and its 14 tests still
pass (75/75 full suite, re-run this phase — see doc 01).

## What this phase COULD reconcile (synthetic, host-level)

For the RISK-04 harness (once it actually runs in CI — see doc 03): each
test's assertions ARE a closed-system reconciliation in miniature —
e.g., `test_recovery_after_short_write_produces_correct_bytes_no_corruption`
proves `bytes written to the fake backend == exactly one clean QRow`,
with the torn 10-byte remnant from the failed attempt fully accounted for
(truncated away, not left as unexplained extra bytes). Every fault-
injection test in `test_queue_fault_injection.cpp` asserts an EXACT
expected byte count / checkpoint value / counter value — there is no
"approximately correct" assertion anywhere in that file.

## What this phase could NOT reconcile

- **No live-device reconciliation** — same limitation as phase 1 (no
  COM6 access this session either).
- **No physical OTA reconciliation** — Physical Tests 1-4 (doc 08) are
  unexecuted; their own acceptance criteria (doc 08) ARE the reconciliation
  identity for when they eventually run (pre/post queue backlog,
  acked_seq, and totalizer values must match modulo new samples generated
  during the test window).
- **RISK-04/RISK-15 test EXECUTION itself** — not run locally (no host
  compiler), pending the new CI job. Cannot report pass/fail counts for
  work that has not run (see docs 03/05 for the honest statement of this
  gap).

## Formal identity, restated for this phase

```
Unique device sequences generated (phase 1's synthetic tests, unchanged)
=
unique server rows stored
+ terminally quarantined rows
+ pending/never-submitted rows
```
Zero unexplained differences (unchanged finding from phase 1, re-verified
by re-running the full 75-test suite this phase with no regressions).

No new instance of this identity was generated by this phase's own work,
since RISK-04/RISK-15 do not touch `push()`/`records`/`quarantined_records`
at all.
